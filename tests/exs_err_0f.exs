// exs_err_0f.exs : the `0f` fixed-point literal prefix was retired with binary fixed
type
    class C
        public
            verb main(player is obj) returns int
                var d is decimal = 0f1.5
                return 0
            endverb
    endclass
