// a source is second-class (sources.md D5): never stored in a field
type
    class C
        private
            s is source of int
        public
            verb main()
            endverb
    endclass
